// RemoteTool entry point (Phase 0 skeleton).
// The GUI, agent listener and tunnel logic arrive in later phases. This
// skeleton exists so the Phase 0 completion definition
// (IMPLEMENTATION_PLAN.md section 5: "two programs can start and exit
// normally") is satisfied and the build graph is exercised end to end.
#include <cstdio>

int main() {
    std::printf("RemoteTool (Phase 0 skeleton)\n");
    return 0;
}
